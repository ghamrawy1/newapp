pipeline {
  
  agent any

stages {

  stage ("bulid") {

steps {
       echo 'build the application'
       }

     }
     
  stage ("test") {

steps {
       echo 'test the application'
       }

     }
     
  stage ("deploy") {

steps{
       echo 'deploying the application'
       }

     }

  }
}
